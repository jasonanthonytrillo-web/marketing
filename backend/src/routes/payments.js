const express = require('express');
const router = express.Router();
const prisma = require('../lib/prisma');

// Xendit has been removed in favor of manual GCash workflow.
// Generic payment routes can be added here in the future.

module.exports = router;
